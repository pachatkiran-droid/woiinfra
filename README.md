# Delvin Realty - Simple React Scaffold

This is a minimal Vite + React scaffold with 4 pages (Home, About, Services, Contact), two navbars (`PrimaryNavbar`, `SecondaryNavbar`) and a `Footer` component. Routing is implemented using `react-router-dom`.

Quick start:

```bash
npm install
npm run dev
```
